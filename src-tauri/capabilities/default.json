{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "packo-*"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "drag:default",
    "dialog:default",
    "opener:default",
    {
      "identifier": "opener:allow-open-path",
      "allow": [
        {
          "path": "$TEMP/cardiganzip-preview"
        },
        {
          "path": "$TEMP/cardiganzip-preview/"
        },
        {
          "path": "$TEMP/cardiganzip-preview/*"
        },
        {
          "path": "$TEMP/cardiganzip-preview/**"
        }
      ]
    }
  ]
}
